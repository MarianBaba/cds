const graph = {
    "A": ["B", "D"],
    "B": ["A", "E"],
    "D": ["A"],
    "E": ["B"]
}

const dfs = (graph, root) => {
    let stack = [];
    let visited = [];
    stack.push(root);
    while (stack.length !== 0) {
        
    }
}

